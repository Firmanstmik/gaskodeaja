services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: gaskodeweb
    volumes:
      - .:/var/www
    networks:
      - db_network

  web:
    image: nginx:alpine
    container_name: gaskodeweb_nginx
    ports:
      - "8006:80"
    volumes:
      - .:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - db_network

networks:
  db_network:
    external: true